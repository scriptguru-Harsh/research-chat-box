
  # Minimal Chat Research Assistant

  This is a code bundle for Minimal Chat Research Assistant. The original project is available at https://www.figma.com/design/jDe7qblJRhYeo30APi7AND/Minimal-Chat-Research-Assistant.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  